# ============================================================
#  EduSphere — Database Configuration
#  SETUP: Copy this file → db.properties, then fill your values.
#  db.properties is in .gitignore and will NEVER be committed.
# ============================================================

db.url=jdbc:mysql://localhost:3306/ums
db.user=root
db.password=YOUR_PASSWORD_HERE
